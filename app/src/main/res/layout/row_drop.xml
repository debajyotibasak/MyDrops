<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="2dp"
    android:layout_marginLeft="2dp"
    android:layout_marginRight="2dp"
    android:layout_marginBottom="2dp"
    android:elevation="@dimen/elevation_elements"
    android:background="@drawable/bg_row_drop"
    android:orientation="horizontal"
    android:padding="@dimen/row_padding">

    <ImageView
        android:layout_width="@dimen/row_drop_size"
        android:layout_height="@dimen/row_drop_size"
        android:background="@null"
        android:src="@drawable/ic_drop" />

    <TextView
        android:id="@+id/row_what"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingLeft="@dimen/row_padding"
        android:paddingRight="@dimen/row_padding"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/row_text_size" />

    <TextView
        android:id="@+id/row_when"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/row_text_size" />

</LinearLayout>